{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build test",
            "type": "shell",
            "command": "cmake -S ${workspaceFolder} -B ${workspaceFolder}/build/tests -DCMAKE_BUILD_TYPE=Debug -Wno-dev && cmake --build ${workspaceFolder}/build/tests",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        },
    ]
}